paths:
  # reuses the same key names as pseudo_query_conf.yaml so the same script works
  msmarco_train: data/nq_text_title/train.jsonl
  pseudo_queries: data/nq_text_title/pseudo_queries.jsonl

gen_pseudo_queries:
  model: doc2query/msmarco-t5-base-v1
  n_queries: 5
  batch_size: 64
  max_doc_len: 256
  max_query_len: 64
  device: cuda
