[project]
name = "iclgr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.5.0",
    "hydra-core>=1.3.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
